VITE_API_BASE=https://phishguard-ai-2-g5ca.onrender.com

# Note: OPENAI_API_KEY is a BACKEND env var (set it on Render for the
# backend service), not a frontend one — it must never be exposed to the
# browser. See backend/.env.example.
